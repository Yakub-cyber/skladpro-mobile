-- ── Realtime для заказов (уведомления в мобильном приложении) ────────────────
-- Включает трансляцию изменений таблицы orders через Supabase Realtime.
-- Мобильное приложение подписывается на изменения заказов своей компании и
-- показывает локальное уведомление (новый заказ / назначен курьеру / доставлен).
-- RLS уже ограничивает видимость своей компанией — realtime это уважает.
-- Запустите в Supabase → SQL Editor → Run. Идемпотентно.

alter publication supabase_realtime add table public.orders;

-- Готово. (Если ругнётся «relation is already member» — значит уже включено, ок.)
